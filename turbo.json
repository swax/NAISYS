{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "client-dist/**"]
    },
    "clean": {
      "cache": false
    },
    "test": {
      "dependsOn": ["build", "^build"],
      "cache": false
    }
  }
}
