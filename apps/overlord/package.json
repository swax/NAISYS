{
  "name": "naisys-overlord",
  "version": "1.0.0",
  "description": "NAISYS Overlord - Management interface for NAISYS agents",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=shared && npm run build --workspace=client && npm run build --workspace=server",
    "start": "NODE_ENV=production npm start --workspace=server",
    "install:all": "npm install && npm install --workspace=server && npm install --workspace=client",
    "format": "prettier --write .",
    "outdated": "npm outdated && npm outdated --workspace=server && npm outdated --workspace=client && npm outdated --workspace=shared",
    "update:all": "npm update && npm update --workspace=server && npm update --workspace=client && npm update --workspace=shared"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.6.2"
  }
}
